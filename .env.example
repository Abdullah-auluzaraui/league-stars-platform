DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/league_stars
DIRECT_URL=postgresql://postgres:CHANGE_ME@localhost:5432/league_stars
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
# Keep disabled unless this database belongs to a separate demo deployment.
DEMO_MODE=false
DEMO_DATABASE=false
NEXT_PUBLIC_DEMO_MODE=false
# Only needed when running npm run admin:bootstrap (never public).
ADMIN_USERNAME=
ADMIN_PASSWORD=
# Optional image uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
